# Task

Write a Python script named `extract.py` in this directory that reads
`log.txt` and writes `emails.txt`, one email address per line, containing
every unique email address found in `log.txt`, sorted alphabetically
(case-insensitive sort, lowercase the output). Do not include duplicates.

Only create `extract.py` and run it so `emails.txt` exists.
