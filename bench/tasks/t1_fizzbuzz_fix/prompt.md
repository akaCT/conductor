# Task

The file `buggy.py` in this directory implements FizzBuzz for 1..30 but has a
bug: it never prints "FizzBuzz" for multiples of 15, it prints "Fizz" instead.

Fix `buggy.py` in place so it correctly prints:
- "Fizz" for multiples of 3 (not 5)
- "Buzz" for multiples of 5 (not 3)
- "FizzBuzz" for multiples of both 3 and 5
- the number itself otherwise

Do not change the output format (one value per line, numbers 1 through 30
inclusive). Only edit `buggy.py`.
